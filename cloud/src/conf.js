
const TEST = true

const baseURL = 'http://127.0.0.1:1337'
const apiURL = baseURL

const ENDPOINT = {
  contact: 'contacts',
  task_sms: 'task-sms',

  category: 'categories',
  mag_group: 'msg-groups',
}

export default {
  baseURL,
  apiURL,

  TEST,

  ENDPOINT,
}
